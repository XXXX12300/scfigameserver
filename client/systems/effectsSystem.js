export class EffectsSystem {
    constructor() {
        this.particles = [];
    }
    spawnExplosion(x, y) {}
    spawnMuzzleFlash(x, y, rotation) {}
    spawnLaser(x1, y1, x2, y2, color) {}
    
    update(dt) {}
    draw(ctx) {}
}
