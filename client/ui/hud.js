export class HUD {
    constructor() {
        // Initialize HTML overlays or canvas positions
    }
    draw(ctx, width, height, playerState) {
        // Draw health, ammo, active weapon, scorestreak bar
    }
}
